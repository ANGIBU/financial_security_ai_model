# requirements.txt
# 현재 제공된 코드 기반 최적화 버전

# 핵심 딥러닝 프레임워크 (현재 코드에서 필수)
torch==2.1.0
transformers==4.36.0
tokenizers==0.15.0
accelerate==0.25.0
bitsandbytes==0.41.3
safetensors==0.4.1

# 데이터 처리 (현재 코드에서 사용)
pandas==2.0.3
numpy==1.24.3
tqdm==4.66.1

# 정규표현식 및 텍스트 처리 (현재 코드에서 사용)
regex==2023.10.3

# 한국어 처리 강화 (현재 시스템 개선용)
konlpy==0.6.0
soynlp==0.0.493

# 과학 계산 (모델 연산 최적화)
scipy==1.11.4
scikit-learn==1.3.2

# HuggingFace 생태계 (모델 로딩용)
huggingface-hub==0.19.4
datasets==2.15.0

# 모델 최적화 (현재 SOLAR 모델용)
peft==0.7.1
sentencepiece==0.1.99
protobuf==4.25.1

# 시스템 유틸리티
typing-extensions==4.8.0
packaging==23.2
filelock==3.13.1
fsspec==2023.10.0
pyyaml==6.0.1

# 오프라인 환경용 (필수)
requests==2.31.0
urllib3==2.1.0
certifi==2023.11.17
charset-normalizer==3.3.2
idna==3.6

# 메모리 관리 (현재 cleanup() 함수용)
psutil==5.9.8

# 설치 도구
setuptools==69.0.3
wheel==0.42.0
pip==23.3.1

# 설치 명령어:
# pip install torch==2.1.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# pip install -r requirements.txt
# 한국어 형태소 분석기 설치 (선택사항):
# python -m konlpy.tag install mecab